name: 05 - 2 - Event filters and activity types

on: 
    pull_request:
        types:
            - closed
            - synchronize
        branches:
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is closed AND base branch is main"

